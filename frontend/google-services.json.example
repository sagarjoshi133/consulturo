{
  "_comment": "▶ THIS IS A PLACEHOLDER. Replace with the real google-services.json downloaded from Firebase Console BEFORE running `eas build --platform android`.",
  "_setup_steps": [
    "1. Visit https://console.firebase.google.com → Create project (free)",
    "2. Add Android app — package name: com.drsagarjoshi.consulturo",
    "3. Download google-services.json and place it at /app/frontend/google-services.json (replace this file)",
    "4. From Firebase Console → Project Settings → Cloud Messaging → copy the FCM Server Key (Cloud Messaging API Legacy)",
    "5. Run: cd /app/frontend && npx eas-cli credentials --platform android",
    "   → Push Notifications → Set up a Firebase Cloud Messaging API Key → paste the FCM Server Key",
    "6. Rebuild: npx eas-cli build --platform android --profile preview",
    "7. Install the new APK — Notifs panel turns green and 'Send test push' will work."
  ],
  "project_info": {
    "project_number": "REPLACE_ME_PROJECT_NUMBER",
    "project_id": "REPLACE_ME_PROJECT_ID",
    "storage_bucket": "REPLACE_ME.appspot.com"
  },
  "client": []
}
