"""ConsultUro service layer.

Domain-specific helper modules consumed by the routers/. Each
service file owns ONE bounded concern (registration-no allocation,
email sending, push dispatch, telegram alerts, etc.) and exposes a
small public API.

Phase 6 of the modularization plan.
"""
